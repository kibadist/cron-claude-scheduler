#!/usr/bin/env bash
# Stub claude verifier: claims it checked the app in the browser and passes.
cat > /dev/null
echo "verifying from: $(pwd)"
echo "ran the app, clicked through the feature, everything works"
echo "VERDICT: PASS"
exit 0
